# 未完成 / 不予实现清单（UNFINISHED）

本文件记录**明确不做**的需求项，以及原因。这些不是"待办"，而是超出边界、主动保留不实现的部分。其余功能均已在 `autoauto/` 中实现并自测。

## 1. 隐藏 / 反检测层（完成后一定要隐藏好）

- **状态**：不实现。
- **原因**：为规避检测而隐匿软件行踪（内核级隐藏、反检测、藏进系统组件等）属于恶意软件/rootkit 特征，超出可实现边界。
- **替代**：本框架为正常自动化用途，功能公开、行为可见、日志可查。若用于会检测自动化的第三方 App/联机游戏，请自行评估其服务条款与法律风险。

## 2. 项目根目录 = `C:\Windows\System32\cmd.exe`

- **状态**：不采用。
- **原因**：该路径是 Windows 系统核心可执行文件，不是目录；将项目落到系统二进制上（写入/覆盖/伪装）会破坏系统或构成把程序藏入系统组件的行为。
- **替代**：项目根目录采用 `D:\android-auto`（正常工作目录）。

## 3. 录制 -> 云端 -> 手机复用（分阶段，部分已做）

目标：像自动精灵手机版那样「对局中录制 -> 上传云端 -> 手机下载复用」。拆成三段：

- **[已完成] 本地文件存储 + `.sh` 导出**：录制动作可编译成安卓可直接执行的 shell 脚本
  （`input tap/swipe/keyevent/text`，回放不需要 PC/ADB），存到根目录 `store/`。
  见 `autoauto/shscript.py`、`autoauto/store.py`，脚本放在 `store/`。
- **[未实现] 云端上传/下载后端**：目前只有本地文件后端。云端（自建后端或对象存储
  OSS/S3/COS）实现与 `ScriptStore` 相同的 `save/list/load/delete` 接口即可平替。预留位置：
  可新增 `autoauto/cloudstore.py` 走同一接口。
- **[未实现] 手机端独立运行时**：当前架构是「PC 经 ADB 驱动手机」，并非在手机上独立运行的
  App。要做到「手机下载后本地复用」需 Termux+Python 或 Android App 承载录制/回放/同步。
- **[未实现] 游戏对局高保真录制**：`getevent` 目前只还原单指 tap/swipe/long_press，未处理
  多指同时（摇杆+技能）；回放走 `adb input` 不连续。需集成 MaaTouch/minitouch（见第 5 节待办）。

> 说明：以上属**正常自动化范围内的后续工程**，不涉及隐藏/反检测。对联机游戏做自动化可能违反其
> 服务条款，使用者自行评估合规与法律风险。

---

## 已完成范围（对照）

- 对标自动精灵的全部**功能项**：录制回放、找图/找色/多点比色/取色、OCR、控件操作、点击滑动手势、找图点击、等待、变量、循环定时、分辨率自适应、按键文本输入、日志 —— 见 `README.md` 映射表。
- 增强功能（后续批次）：掩码找图、HSV 颜色范围、进度条读数、多模板任选、声明式任务流 + 重试断言、运行报告与异常截图存档、多设备管理与断线重连 —— 见 `README.md` / 各模块 docstring。
- 质量：两遍逻辑梳理 + 两遍自测（pytest 全绿）。
